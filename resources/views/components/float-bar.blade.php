<nav
    aria-label="Navegação principal"
    {{ $attributes->class('inline-flex items-center gap-1 rounded-full border border-border-primary bg-background-blur p-1 backdrop-blur-md') }}
>
    {{ $slot }}
</nav>
